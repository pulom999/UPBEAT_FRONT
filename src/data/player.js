export class player {
  constructor(playerIndex, budget, cityCenterPos, crewPos, status) {
    this.budget = budget
    this.cityCenterPos = cityCenterPos
    this.crewPos = crewPos
    this.playerIndex = playerIndex
    this.status = status
  }
}
export default player